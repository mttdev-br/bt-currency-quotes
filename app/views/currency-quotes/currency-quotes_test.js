'use strict';

describe('myApp module', function() {

  beforeEach(module('myApp'));

  describe('currency-quotes controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var currency-quotesCtrl = $controller('currency-quotesCtrl');
      expect(currency-quotesCtrl).toBeDefined();
    }));

  });
});
